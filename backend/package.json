{
  "name": "dining-hall-tracker-backend",
  "version": "1.0.0",
  "description": "Dining hall tracker application backend with frontend build process",
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "cacheDirectories": ["../frontend/node_modules"],
  "scripts": {
    "build": "cd ../frontend && npm install && NODE_ENV=production npm run build",
    "postinstall": "npm run build && mkdir -p public && cp -r ../frontend/dist/* public/"
  },
  "dependencies": {}
} 