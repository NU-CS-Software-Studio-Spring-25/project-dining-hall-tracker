{
  "name": "dining-hall-tracker",
  "version": "1.0.0",
  "description": "Dining hall tracker application with React frontend and Rails API backend",
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "clean": "rm -rf frontend/dist backend/public/dist backend/public/assets",
    "build:frontend": "cd frontend && npm install && npm run build",
    "copy:frontend": "mkdir -p backend/public && cp -r frontend/dist/* backend/public/",
    "copy:backend": "cp -r backend/* . && rm -rf backend",
    "deploy": "npm run clean && npm run build:frontend && npm run copy:frontend && npm run copy:backend",
    "postinstall": "npm run deploy",
    "start": "bundle exec rails server"
  }
} 