{
  "name": "dining-hall-tracker",
  "version": "1.0.0",
  "description": "Dining hall tracker application with React frontend and Rails API backend",
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build && mkdir -p ../backend/public && cp -r dist/* ../backend/public/",
    "deploy": "cp -a backend/. .",
    "postinstall": "npm run build && npm run deploy",
    "start": "bundle exec rails server"
  },
  "cacheDirectories": [
    "frontend/node_modules"
  ]
} 