{
  "name": "dining-hall-tracker",
  "version": "1.0.0",
  "description": "Dining hall tracker application with React frontend and Rails API backend",
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "clean": "rm -rf frontend/dist backend/public/dist backend/public/assets backend/public/index.html",
    "build:frontend": "cd frontend && npm install && NODE_ENV=production npm run build",
    "copy:frontend": "mkdir -p backend/public && cp -r frontend/dist/* backend/public/ && touch backend/public/.nojekyll",
    "copy:backend": "cp -r backend/* . && rm -rf backend",
    "predeploy": "npm run clean",
    "deploy": "npm run build:frontend && npm run copy:frontend && npm run copy:backend",
    "postinstall": "npm run deploy",
    "start": "bundle exec rails server -p ${PORT:-3000}",
    "heroku-postbuild": "npm run deploy"
  }
} 